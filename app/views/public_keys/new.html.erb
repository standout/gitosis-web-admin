<h1>New public_key</h1>

<% form_for(@public_key) do |f| %>

  <%= render :partial => 'form', :locals => { :f => f } %>
  
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', public_keys_path %>